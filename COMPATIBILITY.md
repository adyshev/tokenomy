# Compatibility Notes

- Tokenomy expects a Pi extension environment with `@earendil-works/pi-coding-agent` APIs.
- Model IDs must exist in the selected provider registry.
- Tool management is now opt-in by default; enable it only if you want Tokenomy to change active tools.
- If your model names differ from the defaults, update `.pi/tokenomy.json` accordingly.
